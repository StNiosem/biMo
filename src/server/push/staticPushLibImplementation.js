/* STATIC PUSH LIBRARY
   This might actually work */

import { sleep } from "oAuthTokenImplementation.js";

function PushLibIMPL(pushAddr, pushType, pushContent, pushAuth, pushLang) {
   webkitURL(pushAddr)
   payload =  d_PushAddr.push_content(pushContent, pushType)

   pushType = input("pushType input?")
   passsPushFunctionHandler(input, payload, pushAuth, pushLang)
}